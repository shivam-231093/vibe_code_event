import { FormattedTimeFromMillisecondsType } from './utils/Time';
export type useTimeSettingsType = {
    format?: '12-hour';
    interval?: number;
};
export default function useTime({ format, interval: customInterval }?: useTimeSettingsType): FormattedTimeFromMillisecondsType;
